<h1>All our flats (<%= @flats.length %>)</h1>

<% @flats.each do |flat| %>
  <div>
    <p><%= flat.name %></p>
    <p><%= flat.address %></p>
    <p><%= flat.price_per_night %></p>
    <p><%= flat.number_of_guests %></p>
  </div>
<% end %>
